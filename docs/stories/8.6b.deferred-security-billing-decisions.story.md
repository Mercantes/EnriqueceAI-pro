# Story 8.6b: Diferidos da Auditoria — Decisões de Segurança & Billing

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint"]

## Origem
Fatiamento da Story 8.6 (backlog da auditoria QA de 2026-08-14). Agrupa os itens que **dependem de uma decisão** (produto/segurança/billing) antes de implementar — o usuário optou por adiar em ago/2026. **Não iniciar a implementação sem a decisão registrada.**

## Story
**As a** responsável técnico,
**I want** fechar as decisões pendentes de enforcement de segurança e billing e implementá-las conforme a escolha,
**so that** rotas privilegiadas fiquem default-deny e o acesso pago reflita o estado real de pagamento.

## Complexity
**M/L** — cada decisão é uma sub-implementação independente; risco alto (pode expor/quebrar rota ou bloquear cliente).

## Scope (cada item = 1 decisão → 1 implementação)

### D1 — Middleware default-deny `/api/admin` + `/api/workers` (`src/middleware.ts`)
- Hoje: ambos os prefixes estão no allowlist público (bypass sessão+CSRF); proteção 100% delegada a cada handler chamar `verifyServiceRole`. Rota nova sem guard fica exposta.
- **Decisão necessária:** wrapper `withServiceRole()` por-rota vs. check no middleware.
- Pré-requisito: **inventariar todas as rotas** sob esses prefixes e confirmar que nenhuma legítima quebra.

### D2 — CSRF sem header `Origin` (`src/middleware.ts:~26`)
- Hoje: o check só roda `if (origin)`; mutação sem `Origin` passa.
- **Decisão:** fallback por `Referer` / negar mutações sem `Origin` em rotas sensíveis não-token.

### D3 — Ativação Stripe por `payment_status` (`src/app/api/webhooks/stripe/route.ts`)
- Hoje: `checkout.session.completed` ativa sem checar `payment_status` → boleto/PIX liberam acesso antes de compensar.
- **Decisão (adiada pelo usuário em ago/26):** gatear em `payment_status='paid'` + ativar pendentes via novo handler `invoice.payment_succeeded`. ⚠️ Sem o handler, gatear quebraria pagamentos assíncronos — os dois andam juntos.

### D4 — Enforcement server-side do SubscriptionGuard (`src/features/billing/components/SubscriptionGuard.tsx`)
- Hoje: bloqueio só client-side (contornável); `past_due` não bloqueia.
- **Decisão (adiada):** mover enforcement para o servidor (layout `(app)`/actions) + política de `past_due` (grace period?).

## Dependencies
- **Decisão explícita do usuário por item** antes de implementar.
- D3 depende do índice único de `stripe_customer_id` (Story 8.6a) para resolução confiável.

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| Default-deny quebra rota admin/worker legítima | Média | Alto | inventário + testes; feature flag para rollout |
| Gate de ativação bloqueia cliente legítimo (cartão) | Média | Alto | cartão continua `paid` na hora; testar ramos de `payment_status` |
| Enforcement server-side corta inadimplente sem aviso | Média | Médio | grace period documentado para `past_due` |

## Acceptance Criteria
(Por item, após a decisão.)
1. **D1:** rota nova sob `/api/admin`\|`/api/workers` sem guard NÃO fica acessível sem service role.
2. **D2:** mutação sem `Origin` é tratada conforme decisão.
3. **D3:** boleto/PIX pendente não libera acesso; ativa em `invoice.payment_succeeded`; cartão ativa na hora.
4. **D4:** assinatura `canceled`/`past_due` bloqueada no servidor conforme política.

## Dev Notes
- Contexto e `arquivo:linha` no relatório da auditoria e nas Dev Notes das stories 8.4/8.5 (PR #322).
- ⚠️ **Gate constitucional:** não implementar item sem a decisão correspondente registrada no Change Log.
